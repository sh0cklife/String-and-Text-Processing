// Strings are sequences of characters
// The string data type in C#
// Declared by the <string> keyword
// strings are enclosed in double quotes: string text = "Hello";
// Strings are immutable (read-only) sequences of chars
// Accessible by index (read-only):
// Stack vs Heap memory

//using System.Diagnostics;

//Stopwatch sw  = new Stopwatch();
//sw.Start();
//string text = "";
//for (int i = 0; i < 20000; i++)
//{
//    text += i;
//}
//sw.Stop();
//Console.WriteLine(sw.ElapsedMilliseconds);

//using System.Text;

//StringBuilder sb = new();

//sb.Append("Hello Denis, how are you?");
////sb.Clear();
//sb.Insert(0, "YOOOOOOOOO. ");

//string result = sb.ToString();
//Console.WriteLine(sb);
//Console.WriteLine(sb.Length);